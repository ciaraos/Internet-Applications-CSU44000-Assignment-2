{
  "name": "internet-applications-csu44000-assignment-2",
  "version": "1.0.0",
  "description": "A simple client (employing Vue.js) interatcing with a server (implemented in Node.js) which in turn interacts with a Cloud-based Database (using AWS DynamoDB) and an Object stored in the Object-store (using AWS S3)",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.804.0",
    "express": "^4.17.1"
  }
}
